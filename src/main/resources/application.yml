spring:
  jpa:
    hibernate:
      ddl-auto: none

calendar:
  github:
    username: ${calendar-github-username}
    password: ${calendar-github-password}
    organizations:
      - micrometer-metrics
      - r2dbc
      - reactor
      - spring-cloud
      - spring-gradle-plugins
      - spring-io
      - spring-projects
  release:
    project_aliases:
      'Toolsuite Distribution': STS
